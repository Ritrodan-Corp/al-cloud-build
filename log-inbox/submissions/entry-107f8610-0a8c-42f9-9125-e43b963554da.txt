2026-09-17 PDT — Oracle direct-Mesa re-test Step 1: host/runtime preflight
Workstream: direct Mesa llvmpipe Oracle re-test
Action: Reconnected to alcloud-a1-01 through RDC and performed the bounded preflight before any renderer mutation.
Result/evidence: Host is online as Linux 6.8.12-alcloud39 on aarch64 with 4096-byte pages, 4 OCPUs, ~16 GiB RAM, and ~17 GiB free on the 96 GiB root filesystem. Docker and alcloud-rdc are running. Current ReDroid init command confirms guest GPU mode, memfd, 1280x720, and fps=30. ADB reports sys.boot_completed=1, ro.hardware.egl=angle, ro.hardware.vulkan=pastel, physical size 1280x720, and the official com.YoStarEN.AzurLane package remains installed at its persisted /data/app path. SurfaceFlinger identifies the current renderer as ANGLE over Vulkan SwiftShader Device (LLVM 10.0.0). The current launcher script binds /var/lib/alcloud-redroid/data -> /data. Historical guarded 60Hz scripts and restore evidence remain present. RDC connector policy still blocks direct sudo/privileged Docker commands, matching the prior documented constraint.
Interpretation/next: Preserve the existing runtime as rollback. Obtain the exact Gate 1/2 Mesa deployment wiring and transfer the verified artifact from GCP, then stage a guarded root script that can perform the renderer A/B with automatic restoration.