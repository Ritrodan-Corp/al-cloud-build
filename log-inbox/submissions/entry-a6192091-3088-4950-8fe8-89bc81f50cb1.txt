Browser software-keyboard suppression validation: opened Android Settings search through the new touch path, confirmed `dumpsys input_method` reported mInputShown=true, invoked /ime-hide, then confirmed mInputShown=false. Azur Lane MainActivity was restored afterward. This demonstrates the controller can suppress the Android soft keyboard only when it is actually visible while retaining the browser/physical-keyboard input path.