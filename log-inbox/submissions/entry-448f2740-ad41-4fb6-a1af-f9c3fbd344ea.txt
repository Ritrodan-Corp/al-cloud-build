2026-09-17 PDT — Oracle direct-Mesa re-test Step 3: guarded one-shot test staged
Workstream: direct Mesa llvmpipe Oracle re-test
Action: Staged /home/ubuntu/mesa-control/gpu_config_mesa.sh and /home/ubuntu/mesa-control/run-mesa-control.sh on Oracle. The test wrapper was written in bounded chunks, passed bash -n, and was not executed during staging.
Result/evidence: run-mesa-control.sh SHA-256 is 75a5b99385d317df9f1fa3fa13435d15aa9d7fe5b2145c43aea7b890c853f376; gpu_config_mesa.sh SHA-256 is 01022196ee8e709998bcd87e063b229c9c25e2c98a21e701e7525deded26937f. The wrapper requires root, re-verifies all five exact Gate 1/2 library hashes, requires kernel 6.8.12-alcloud39 / 4-KiB pages / current 1280x720@30 persistent-data baseline, stops and renames the current ANGLE/Pastel container for rollback, boots an otherwise matched ReDroid container with direct Mesa mounts and llvmpipe, verifies renderer identity, persistent /data, display mode, package presence, package-scoped NATIVE_HEAP_POINTER_TAGGING disable, and <2^39 address layout, then cold-launches Azur Lane for 90 seconds while sampling PID, screenshots, process maps and logcat. It explicitly checks that the game maps /vendor/lib64/libgallium_dri.so and scans for the historical LuaJIT lightuserdata panic, destroyed-mutex, SIGABRT and SIGSEGV signatures. An EXIT trap always removes the Mesa container, renames and restarts the original container, and validates ANGLE/Pastel plus the persistent data mount before exit.
Interpretation/next: Execute this exact one-shot wrapper through an authorized privileged shell. RDC connector policy still rejects privileged Docker execution even though the host user is in sudoers; this is the same previously documented boundary from the 60-Hz experiment. After the single root invocation finishes, inspect the evidence directory and classify Mesa compatibility.