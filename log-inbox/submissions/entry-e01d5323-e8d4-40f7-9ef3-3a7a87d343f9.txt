Added `agent-tests/delegated-observe-probe.json` on vm-control as the tiny durable state object for the Work/subagent experiment. It tells the bootstrap run exactly which VM to target, the single allowed `/usr/local/bin/azl-agent observe` command, how to read the returned screenshot path, the no-input safety constraints, and the compact structured result fields expected from the delegated worker. Next: create a dedicated quiet GitHub PR event surface for this delegation probe so the existing comment-report Work task does not consume a second invocation.