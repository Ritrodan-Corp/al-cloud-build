Logging recovery path transition. The first attempt to add a new resilient file-drain workflow preserved the desired skip-on-rejection behavior but its Google Workload Identity authentication was denied because the provider is deliberately scoped to existing authorized workflow identities. The recovery will therefore use the repository's pre-existing centralized AL log intake, which is isolated from the poisoned immutable-file queue, while the original file-drain path is repaired without widening cloud identity permissions.