Oracle browser-streaming stale-encoder prevention patch deployed locally: added cleanup_stale_screenrecord(), which best-effort pkills Android processes matching the browser stream screenrecord command and waits 200 ms for codec/resource release. stream_h264() now runs ensure_adb() then cleanup_stale_screenrecord() before marking the stream active and launching a new encoder. Because stream_lock has already been acquired and no server-owned stream can exist at that point, any pre-existing matching Android screenrecord is stale. Python compilation passed. Next: externally restart the streamer while a stream is active, intentionally recreating the prior orphan condition, and verify the next reconnect preflight removes the orphan and leaves exactly one encoder.