User manually changed the Compute Engine graceful-shutdown fallback from 600 seconds to 60 seconds after the routine no-delay STOP path had already been implemented and live-validated. Canonical Project State and Technical Reference were updated to reflect the current 60-second fallback while preserving the earlier acceptance evidence that the VM still reported 600 seconds during workflow run 34918092477. Routine `stop` behavior is unchanged: after guest cleanup/baseline verification it uses `noGracefulShutdown=true` and requires STOPPING or TERMINATED. `stop_graceful` remains the conservative fallback when cleanup status is unknown, now with a configured maximum grace period of one minute.