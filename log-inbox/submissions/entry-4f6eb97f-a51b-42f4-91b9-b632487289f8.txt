Oracle PAYG VM setup: user reached optional Block volumes section during instance creation. No additional block volume should be attached during initial launch. The 100 GB boot volume is already configured separately; keeping the Block volumes list empty preserves the current bounded-storage plan and avoids creating an extra data volume before the host/kernel validation phase. Next: continue instance creation with no attached block volumes, then configure SSH key/review/launch.