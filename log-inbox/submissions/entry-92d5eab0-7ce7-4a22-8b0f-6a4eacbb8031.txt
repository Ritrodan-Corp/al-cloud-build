2026-09-16 PDT — Issue #37 backfill: Oracle host/admin foundation
Workstream: Oracle migration / host administration
Source: https://github.com/Ritrodan-Corp/al-cloud-build/issues/37

Action/result: Backfilled the durable host-administration milestones that were written to issue #37 instead of the authorized raw-log intake. Oracle validation VM `alcloud-a1-01` was created and became reachable privately through OCI Bastion; the VM reported private IP `10.42.10.50` and a ~95.82 GiB root filesystem consistent with the planned 100 GB boot volume. Direct host inspection verified Ubuntu 24.04.4 LTS, aarch64, Oracle kernel `6.17.0-1018-oracle`, 4096-byte pages, 4 CPUs, ~15.6 GiB RAM, Node 22.23.2, and npm 10.9.8.

Remote Desktop Commander 0.2.50 was paired and then made durable as `alcloud-rdc.service`, enabled/active under systemd as user `ubuntu` with `Restart=always`. Crash recovery was tested by killing the client and confirming the same device session returned online and responded to a fresh ping without SSH intervention. Tailscale 1.102.4 was installed and enrolled; Tailscale SSH is enabled, `ubuntu` is operator, and `tailscaled` is enabled/active. This establishes a durable backup management path; OCI Bastion remains bootstrap/break-glass rather than the routine administration path. Docker was installed and active before Android qualification.

Interpretation/next: Oracle has advanced from partial network-only infrastructure to a live private validation host with durable primary RDC administration and independent Tailscale SSH recovery. Preserve the private-control boundary.