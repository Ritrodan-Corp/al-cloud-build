2026-09-16 issue #37 reconciliation — Step 1: reviewed the full Oracle migration/provisioning thread on Ritrodan-Corp/al-cloud-build issue #37 and classified it as a missing operational shard that contains durable state not yet promoted into the canonical running log. Durable findings: private OCI VM alcloud-a1-01 exists in the San Jose home region at private IP 10.42.10.50 with Ubuntu 24.04.4 aarch64, 4 CPUs, ~15.6 GiB RAM, ~96 GiB root filesystem from the 100 GB boot profile; Remote Desktop Commander 0.2.50 was made durable as an enabled systemd service running as ubuntu with Restart=always and passed crash-restart/ping validation; Tailscale 1.102.4 is enrolled with Tailscale SSH enabled as an independent recovery path, leaving OCI Bastion recovery-only. Stock Oracle kernel 6.17.0-1018-oracle is 4 KiB but 48-bit VA (CONFIG_ARM64_VA_BITS=48); stock-kernel ReDroid boots, but the official Azur Lane 9.3.7 package reproduced the fast Pastel/LLVM destroyed-mutex crash loop seen outside the qualified low-VA environment. A custom 6.8.12-alcloud39 kernel was built from the Oracle baseline with 39-bit VA/4-KiB pages, installed with the signed Oracle kernel retained as fallback, and successfully booted. ReDroid then booted fully after correcting binderfs device permissions to 0666. The exact Play-derived Azur Lane 9.3.7 base APK transferred from GCP launched stably under the 39-bit kernel with the prior fatal crash signature absent. The base APK alone lacked the full asset payload; a validated APKPure XAPK (1,625,111,891 bytes, SHA-256 534ae5e8206b45fea5e139ebb0cb0bd69c91154ef4971358eec3c0a4a03187f2) matched the known Play-derived signer, installed one APK plus the ~1.4 GB OBB, and produced visible normal game loading/user-agreement UI with stable Azur Lane process. Host-side updater automation now supports metadata/check/stage/install/update, verifies package/version/signature/hash, preserves app data with ADB update semantics, deploys OBB payloads, retains up to three versioned XAPK rollback candidates, uses a host-side exclusive lock, and has a daily staging-only cron check that works without ReDroid online. The large first-run Yostar asset download was still in progress in the later issue comments. Issue comments are treated as source evidence for this reconciliation; transient failed commands, ETA chatter, and superseded crontab-based RDC persistence are not being promoted as current truth. Accidental connector issue #41 was immediately renamed/closed as not planned and contains no project evidence.