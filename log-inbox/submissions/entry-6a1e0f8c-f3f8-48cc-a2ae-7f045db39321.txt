Made the validated VM-local observation interface reproducible by adding agent-interface/azl-agent to the vm-control branch. The checked-in CLI matches the live /usr/local/bin/azl-agent behavior and currently exposes only the read-only `observe` command, deliberately keeping the first delegated-worker test narrow. Next: configure the Work bootstrap test so a fresh Work invocation spawns an explicit cheaper subagent, restricts it to this observation path, and returns structured evidence.