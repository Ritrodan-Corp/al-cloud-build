Browser UI edge-input diagnosis
Inspected the live browser controller after user feedback. Confirmed all three overlays are currently linked through one global controls-visible class, and the pointer coordinate mapper uses the full replaced-element bounding box even though object-fit: contain letterboxes the rendered Android frame. This explains both issues: moving anywhere reveals all overlays, and taps near the visible left/right Android edges are mapped through the surrounding pillarbox area rather than the actual contained frame. Next: split overlay visibility by independent edge-proximity zones and map pointer coordinates against the true contained video rectangle before resuming agent-control work.