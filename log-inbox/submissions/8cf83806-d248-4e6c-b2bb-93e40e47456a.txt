Decisive immutable GitHub inbox validation after switching change detection to GitHub commit metadata. This log-only submission must set log_intake=true and lifecycle=false, skip VM lifecycle execution, authenticate the centralized writer inside the already-authorized gcp-vm-chat-control workflow, drain every immutable inbox item in creation order, append each report exactly once to the Drive raw log using stable github-file submission IDs, and require no VM-local Google credential or raw-log ID coordination.