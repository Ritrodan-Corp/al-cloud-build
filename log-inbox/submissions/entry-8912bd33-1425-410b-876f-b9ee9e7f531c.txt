Windows launcher documentation step: added `windows-launcher/README.md` explaining the normal double-click `Azur Lane Cloud.vbs` path, visible cmd fallback, prerequisites, automatic localhost reuse, failure log location, background tunnel lifetime, and the deliberate preservation of the VM's loopback-only browser service. This documents the quick one-click solution while leaving Cloudflare as a later optional access layer.