2026-09-14 — Timer/wake proof prerequisites clarified.

For the first concrete timer/wake experiment, no Android/VM changes are required. We can begin by implementing the control-plane code in the existing vm-control branch. Deployment will require a Cloudflare account-side action because no Cloudflare ChatGPT plugin/connector is available in the current environment. Proposed first proof remains: Sol schedules one wake through a tiny control-plane API, Cloudflare stores it and fires a Durable Object alarm, the alarm emits a GitHub event, Work wakes, acknowledges the event, and schedules the next wake. GitHub is preferred for the initial wake transport because the repository/connector path already exists. User-side prerequisites are limited to authorizing/deploying the Cloudflare Worker and, if needed, adding deployment credentials/secrets; no paid Android VM start is justified for this phase.