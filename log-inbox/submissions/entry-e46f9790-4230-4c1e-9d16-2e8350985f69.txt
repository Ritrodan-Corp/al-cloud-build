2026-09-17 PDT — Mesa artifact recovery closeout and low-risk rebuild optimization review
Workstream: direct Mesa llvmpipe re-test preparation
Action: Closed the GCP artifact-recovery session after exact bundle verification, reviewed the original gate1-mesa build workflow, and checked current Mesa/Meson and Ampere guidance for low-risk optimization candidates. No Android runtime or game process was launched. GCP was returned offline after the search.
Result/evidence: The original control does not need rebuilding. The Gate 1 workflow built LLVM 19.1.7 with CMAKE_BUILD_TYPE=MinSizeRel and generic -march=armv8-a, while Mesa used buildtype=release without explicitly setting Meson b_ndebug. Current Mesa documentation states that release buildtype does not itself disable assertions; Meson supports b_lto and thin LTO; OCI A1 uses Ampere Altra, and Ampere recommends -mcpu=neoverse-n1 for Altra/Altra Max. Mesa also exposes LP_NUM_THREADS as a runtime llvmpipe thread-count control, defaulting to detected CPU cores.
Interpretation/next: Preserve the exact recovered Gate 1/2 bundle as the control. If it qualifies on Oracle, produce separately versioned optimized candidates rather than altering the control. The simplest first candidates are: (1) Mesa b_ndebug=true, (2) target native code for Neoverse N1 instead of generic armv8-a, (3) LLVM Release instead of MinSizeRel, and (4) Mesa ThinLTO. Test LP_NUM_THREADS separately as a runtime knob because the four-core host must share CPU with Unity and Android.