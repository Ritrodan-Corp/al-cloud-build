Oracle FPS diagnostics: SurfaceFlinger latency query on the parent Azur Lane SurfaceView returned no presentation timestamps. Layer enumeration showed the actual buffer producer is the SurfaceView BLAST layer. The first broad layer loop also exposed a shell-quoting issue for the BLAST layer name because it contains parentheses. Next: query the BLAST layer with explicit remote-shell quoting and compute compositor presentation FPS from those timestamps.