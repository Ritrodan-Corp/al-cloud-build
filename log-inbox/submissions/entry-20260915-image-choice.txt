Oracle VM creation checkpoint: clarified that the source image matters. Keep the deployment on Ubuntu 24.04 ARM64/aarch64 to minimize variables versus the already-qualified Linux userspace/tooling path. Prefer the Minimal variant if available; standard Ubuntu is also acceptable. Avoid x86_64 images and avoid switching to Oracle Linux unless there is a specific reason, because that would add package/kernel-tooling differences before the custom low-VA kernel qualification.