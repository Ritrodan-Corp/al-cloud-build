Browser streaming offline build verification completed. Prototype on vm-control now includes a stdlib-only localhost Python server, raw Annex-B H.264 chunked HTTP transport, browser WebCodecs decoder/canvas renderer, preserved tap/swipe/key/text/settings/app-info/density APIs, launch/status endpoints, reconnect handling, one-client encoder ownership, and PNG fallback. Readback caught and fixed two robustness issues: pointer controls now work on PNG fallback as well as the live canvas, and decoder error/backpressure aborts the current transport so reconnect starts from a fresh screenrecord SPS/PPS/IDR keyframe. Exact repository server code compiles and its four Python unit tests pass; exact repository player/test blobs pass all three Node parser/assembler tests; updated app.js passes Node syntax validation and its local tested Git blob matches the committed blob e6abbea6387c128ce4bbb1d50d2e6ae5059dd720. W3C WebCodecs AVC registration was rechecked: absent VideoDecoderConfig.description selects Annex-B input, and Annex-B key chunks are expected to contain the IDR plus necessary parameter sets, matching this implementation. Live VM/browser integration is still required before qualification or promotion to the existing port-8765 controller.
