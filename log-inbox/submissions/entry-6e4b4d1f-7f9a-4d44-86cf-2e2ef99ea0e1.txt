Canonical reconciliation completed through raw-log R000218.

Project State now reflects the settled Experiment 157 performance pass and guest-side closeout state. The stale current 2-vCPU/15-FPS rollback-container wording was removed. Canon now records that display-size reduction materially lowers software-renderer CPU, that a two-CPU CFS quota severely understates dedicated-core performance because of periodic throttling, and that 854x480 at 12 Hz on two dedicated host cores is the first tested configuration that both preserves the 12-FPS floor and stays below the 180% CPU ceiling with bounded H.264 encode headroom around 2 Mbps. It also records that the temporary 10-Hz and 12-Hz performance containers were removed, the retained 15-FPS backup remains stopped, and dedicated persistent Experiment 157 ReDroid/controller services were restored with boot=1, loopback-only ADB/controller, ANGLE/pastel, default 1280x720 physical display with no logical override, the persistent exp157 data mount, and protected reference data untouched. The only remaining Experiment 157 closeout action is the required graceful VM STOP.

Technical Reference now preserves the new performance evidence from R000214-R000217 and the restored guest state from R000218. Durable results include unrestricted 30-Hz CPU scaling from 1280x720 to 1024x576 and 960x540; the 960x540 two-CPU CFS-quota versus dedicated-cpuset comparison; 960x540 display-rate tests at 15/12/10 Hz; bounded H.264 feasibility; and the 854x480/12-Hz two-dedicated-core result at about 156.2% mean CPU without encoding and about 161.1% with encoding while maintaining 12 FPS. Canon explicitly treats these local screenrecord tests as encoder feasibility only, not end-to-end browser/WebSocket latency validation, and recommends dedicated-core placement or an actual A1 instance for future Oracle prediction rather than CFS quota alone.

Objectives remain unchanged because their performance/manual-access goals are still valid. Experiment History remains unchanged because Experiment 157 is not yet closed until the graceful VM STOP completes. No R000219 existed at the final pre-submit tail check.