Browser UI refinement repository-sync step: committed the deployed full-screen index.html and style.css to vm-control. The repository now matches the live markup/styling direction: no title/header waste, compact HUD with LIVE on the same row as controls, bottom navigation pill, contextual text input, and auto-hiding overlays over a viewport-filling stream. Server source sync remains outstanding for the newly deployed /touch and /ime-hide handlers.