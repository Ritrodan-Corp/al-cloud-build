Oracle VM setup checkpoint: OCI image selector showed Ubuntu family but an exact search for 'Ubuntu 24.04 ARM64' returned no results because OCI image names use the architecture label 'aarch64'. Oracle's current image catalog publishes Canonical-Ubuntu-24.04-Minimal-aarch64 and Canonical-Ubuntu-24.04-aarch64. User should search for '24.04' (or 'aarch64') and prefer the Minimal aarch64 image, falling back to standard 24.04 aarch64 if needed. Do not fall back to 22.04 solely because it is visible higher in the list.