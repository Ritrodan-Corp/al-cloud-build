Browser-controller keyboard probe: current IME is `com.android.inputmethod.latin/.LatinIME`; `show_ime_with_hard_keyboard` is 0; `dumpsys input_method` exposes `mInputShown` (currently false). There is no direct `ime hide` shell command in this build. A safe suppression design is to detect IME visibility and dismiss it only when shown, while continuing to inject text through ADB/browser controls. Next: implement contextual keyboard suppression without globally disabling the IME.