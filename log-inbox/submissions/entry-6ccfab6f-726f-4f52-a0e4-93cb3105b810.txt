Canonical reconciliation completed through raw-log R000206. Project State and Technical Reference now record the recovered Experiment 157 target-size result: the unmodified official client completed a representative user-started Tales of the Paranormal C3 built-in auto-run at 2 vCPU / 15 FPS, with repeated combat/result/level transitions, final Auto-Repeat finished 1/1 confirmation, stable game PID, no game-fatal/Lua/destroyed-mutex/network-timeout markers, about 196% average container CPU during the strict measured combat window, and about 2.66 GiB average memory. Canon explicitly does not claim direct combat presentation FPS because final SurfaceFlinger combat history was unavailable. It distinguishes functional unattended/autoplay viability from severe menu/navigation latency under CPU saturation.

Canon also now records the deployed production logging architecture rather than the transitional direct-Docs facade: experiment agents create immutable `log-inbox/submissions/entry-<uuid>.txt` reports on `vm-control`; the path-gated centralized GitHub/WIF drainer skips VM lifecycle for log-only commits, owns ordering and permanent RNNNNNN allocation through internal `tools/al-log-append.py`, and uses stable submission IDs for idempotent retries. VM-local Google credentials are not required. The main repository guide `tools/RAW_LOG_ARCHITECTURE.md` was aligned at commit 11824942208052343e6d580e17d8c5a03c249fea, and the AL Cloud Raw Log header contract was updated to the same production workflow.

Experiment History remains unchanged because Experiment 157 is still active pending clean runtime/service closeout. Its compatibility, persistence, and Oracle-like target resource-floor discriminators are now treated as passed. Next: restore the intended persistent Experiment 157 service state or protected reference baseline as appropriate, verify boot/ADB/controller state, and gracefully stop the VM when guest-required work is complete; smoother remote streaming remains separate post-compatibility usability work.