Oracle browser-streaming black-frame triage: user reported intermittent black output while the page still displayed Live H.264, 0.0 fps, decode queue 0, and ADB up. A fresh /screen.png capture from the same Oracle/ReDroid session rendered the Azur Lane Terms of Service screen correctly at 1280x720, proving the Android/game surface is capturable and isolating the fault to the live H.264 transport/decoder path. At the time of the first server probe, /api/status showed stream inactive with only 29498 bytes sent and no last_error. Azur Lane PID 70121 remained alive. Next: reproduce the H.264 stream server-side, measure byte continuity and NAL/keyframe cadence, then patch the streaming path rather than the Android renderer.