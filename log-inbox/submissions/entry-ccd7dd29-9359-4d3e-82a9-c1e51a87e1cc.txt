2026-09-17 PDT — Init-level four-core Mesa A/B staged and audited
Workstream: direct Mesa llvmpipe Oracle performance optimization
Action: Built `/home/ubuntu/mesa-control/run-mesa-perf-initlp4.sh` from the known-good default Mesa performance wrapper, not from the ineffective Docker-env LP4 variant, and completed an exact diff audit.
Result/evidence: Wrapper SHA-256: `0c248549a3a43614b8a7ba2687c4b12e82dfd43c3c8985947acb2036e3f7069f`. It keeps the exact Gate 1/2 Mesa libraries, persistent /data bind, low-VA/package compatibility gates, GALLIUM_DRIVER=llvmpipe, and 1280x720@30. It bind-mounts the audited one-line rc variant `a54e084b...4a3dd`. Before SESSION_READY it now hard-gates on `LP_NUM_THREADS=4` being present in SurfaceFlinger `/proc/<pid>/environ` and the Azur Lane process environment; otherwise it aborts and restores. Remaining diff lines only isolate evidence/log/sentinel names and success marker. Baseline at staging was boot_completed=1, egl=angle, vulkan=pastel, physical size 1280x720.
Interpretation/next: Launch this init-level wrapper through the authorized SSH sudo boundary. If both environment gates pass, immediately sample host per-thread CPU. Only proceed to home FPS if rendering visibly spreads across multiple cores.