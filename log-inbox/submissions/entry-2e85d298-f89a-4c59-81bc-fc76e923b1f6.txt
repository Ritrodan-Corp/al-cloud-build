2026-09-17 PDT — Mesa performance/menu session 1 partial qualification and clean restore
Workstream: direct Mesa llvmpipe Oracle performance/menu qualification
Action: Ran the first extended Mesa performance/menu session at 1280x720@30 using the exact Gate 1/2 control. Navigated only startup/title/login flow, with no combat. Identified and confirmed the post-resource modal by visible button geometry, allowed background resource/version finalization to complete, skipped the opening movie, then used the visible saved-account login control.
Result/evidence: Direct Mesa remained stable with game PID 1646 throughout the session. The resource finalization reported all background tasks complete and wrote the current resource version state. LoginUI2 loaded; after the saved-account login action the client completed the authentication/gateway exchange and logged successful user login. One `OPENGL NATIVE PLUG-IN ERROR: GL_INVALID_OPERATION` was emitted by the opening-video native plug-in, but it did not kill or restart the game and ordinary rendering resumed after skipping the movie. No historical LuaJIT lightuserdata panic, destroyed-mutex, SIGABRT, or SIGSEGV was observed during this work. The session's 900-second interaction guard expired at 04:12:12Z before a valid matched home-screen SurfaceFlinger benchmark or menu-navigation set could be completed; a benchmark command begun at the timeout boundary produced no usable data and is discarded. The wrapper automatically restored the original runtime; at 04:12:50Z Android was booted with egl=angle, vulkan=pastel, physical size 1280x720, and the persistent /data bind mount intact. Exit rc=13 reflects the intentional guard timeout rather than a Mesa/game crash.
Interpretation/next: This extends Mesa compatibility evidence through resource finalization, opening-video handling, and successful saved-account authentication, but does not yet qualify home-screen performance or ordinary menus. Increase the guarded interaction window for the retry; persistent resource-version finalization from this run should reduce startup delay. Reuse the same exact Mesa control and skip combat.