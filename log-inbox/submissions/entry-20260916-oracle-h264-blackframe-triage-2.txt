Oracle H.264 black-frame reproduction: with no active browser stream, requested /stream.h264 locally for 12 seconds and sampled /api/status once per second. The stream emitted 29498 bytes during the first second and then emitted zero additional bytes for the remaining ~11 seconds while stream_state.active stayed true and last_error stayed null. curl timed out at 12 seconds with exactly 29498 bytes. The resulting file is valid H.264 baseline level 4.1. During the stall, browser reconnect attempts were rejected as concurrent clients, increasing clients_rejected from 0 to 9. This reproduces the user's intermittent 0-fps state and shows the current failure is a live screenrecord producer stall, not Tailscale, HTTP transport, or ADB loss. Next: inspect NAL composition and choose watchdog/restart versus capture-source replacement.