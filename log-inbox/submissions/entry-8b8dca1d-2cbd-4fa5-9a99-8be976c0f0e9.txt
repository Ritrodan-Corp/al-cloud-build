Browser continuous-scroll validation: exercised the new /touch DOWN/MOVE/UP path against Android Settings. UIAutomator hierarchy changed across the in-progress drag, and the post-drag hierarchy exposed additional list items (`Apps`, `Recent apps, default apps`, `Notifications`, `Notification history, conversations`) that were not visible before, confirming that separate motionevent requests form a real scrolling gesture rather than merely waiting for pointer release. Azur Lane MainActivity was restored foreground after the test. This validates the continuous-drag mechanism before user refresh.