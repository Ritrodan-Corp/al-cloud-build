Oracle PAYG provisioning checkpoint after Work usage exhaustion.

Work created the supporting OCI network resources in the US West (San Jose) home region: VCN al-cloud-vcn, private regional subnet al-cloud-private-subnet (10.42.1.0/24), NSG al-cloud-instance-nsg with SSH ingress from the private subnet, and Bastion alCloudBastion. Work verified the region is the tenancy home/active region and that Block Volume Free Tier Volume Quota is 200 GB total, 0 GB used, 200 GB available. OCI still showed a $4.25/month untiered estimate for the prepared 100 GB Balanced boot volume. Work then exhausted its usage before creating the NAT path, SSH key, or compute instance. No compute instance, NAT gateway, extra block volume, load balancer, or public VM IP was created. Next: user manually completes SSH-key generation and instance launch, then adds outbound-only internet access for the private subnet and verifies connectivity.