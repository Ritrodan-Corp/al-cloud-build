2026-09-17 PDT | Oracle RDC persistence diagnosis
Workstream: Oracle host administration / RDC recovery
Action: Re-paired Remote Desktop Commander on alcloud-a1-01 after the previous device connection was removed, then checked the newly registered device from ChatGPT after the user's SSH session closed.
Result/evidence: OCI still shows alcloud-a1-01 running. A new RDC device registration for alcloud-a1-01 exists and reports auth_token=valid, but direct RDC ping/tool calls time out after the SSH session closes. This reproduces the practical loss of control despite a nominally registered device. Earlier project history already established that an interactive/crontab/nohup RDC client can die with SSH logout, while the intended durable configuration is the system-level alcloud-rdc.service running as ubuntu with Restart=always.
Interpretation/next: Treat the current pairing as not yet durable. Re-authorize the systemd-owned alcloud-rdc.service process itself rather than an interactive `desktop-commander remote` process, ensure the unit has stable HOME/config paths and network-online ordering, then verify that the service MainPID is systemd-owned and that RDC remains responsive after SSH logout. Do not begin the Mesa runtime experiment until this control-path gate passes; once it survives logout and a fresh RDC ping succeeds, no additional arbitrary waiting period is required.