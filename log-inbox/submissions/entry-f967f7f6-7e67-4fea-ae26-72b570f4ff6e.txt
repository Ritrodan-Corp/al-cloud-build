Checked in `agent-tests/work-bootstrap-delegated-observe-prompt.md`, the exact Work event-task instructions for the narrow delegation experiment. The prompt targets PR #6, requires the parent Work invocation to read the durable probe state from vm-control, spawn exactly one `gpt-5.6-luna` subagent with `fork_turns: none` and the model's lowest supported reasoning effort, forbids fallback to a stronger model, and restricts the worker to `/usr/local/bin/azl-agent observe` plus reading the returned screenshot path. Next: create/enable that Work event task in the ChatGPT Work UI, then post one wake comment to PR #6 and inspect the run result.