2026-09-17 PDT — Oracle Mesa re-test start blocked at VM power state
Workstream: direct Mesa llvmpipe Oracle re-test
Action: Began the requested bounded Mesa compatibility test. Confirmed the exact Gate 1/2 control artifact is preserved and ready on GCP, then checked all available project control paths for bringing Oracle alcloud-a1-01 online. Searched the raw project log, GitHub control surfaces, plugin directory, and GCP host for an OCI CLI/config/start helper. Also checked whether Oracle might merely have a disconnected RDC agent rather than being intentionally stopped.
Result/evidence: Oracle Remote Desktop Commander device alcloud-a1-01 remains offline. The project has a constrained lifecycle bridge for GCP but no equivalent OCI lifecycle bridge. No Oracle/OCI connector is installed, GCP has no oci CLI, no /home/.oci configuration, and no Oracle-start helper. The GCP host also has no Tailscale client/DNS path to recover Oracle independently. Therefore the runtime test cannot begin until the Oracle VM itself is powered on through OCI. No Oracle runtime, ReDroid container, persistent /data, or game state was modified.
Interpretation/next: Once alcloud-a1-01 is powered on and RDC reconnects, immediately run the bounded direct-Mesa control test under kernel 6.8.12-alcloud39, then restore ANGLE/Pastel and log the result.