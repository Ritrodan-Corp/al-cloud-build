Oracle FPS diagnostics matching no-stream CPU sample completed. With Azur Lane open and the browser stream inactive, total host CPU busy averaged 98.3% over ten one-second samples. Azur Lane itself averaged 388.3% CPU on the four-OCPU host. This confirms the VM/game path is already essentially CPU-saturated without streaming. The active-stream sample remained similarly saturated but game CPU fell to ~352% and presentation FPS fell from 16.085 to 14.766, indicating the stream competes for CPU on an already saturated host. Next: identify the current game scene so these FPS numbers are properly scoped.