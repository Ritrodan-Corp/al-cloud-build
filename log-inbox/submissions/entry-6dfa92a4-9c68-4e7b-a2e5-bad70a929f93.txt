2026-09-17 PDT — Oracle direct-Mesa re-test wrapper SIGPIPE fix
Workstream: direct Mesa llvmpipe Oracle re-test
Action: Investigated the first privileged wrapper run after the user-provided terminal output showed rc=141. Read the generated Oracle test log/evidence, confirmed the exact artifact and baseline gates passed, and localized the abort to a pipefail/SIGPIPE interaction in the baseline display probe before any Mesa container was created. Patched every analogous early-closing pipeline in the wrapper: dumpsys display and SurfaceFlinger output are now captured to files before grep -m1, and the game-map sample no longer uses grep|head. Re-ran bash -n and searched the wrapper for remaining docker-exec-to-grep-m1 or head pipelines.
Result/evidence: The first run log /home/ubuntu/mesa-control/mesa-oracle-test-20260918T034436Z.log ends immediately after the baseline DisplayDeviceInfo line with restore start rc=141; the evidence directory contains only baseline/restored package and display files, proving Mesa never started. Current Android remains boot_completed=1 with egl=angle and vulkan=pastel. Patched wrapper SHA-256 is 0f724c7b968096a616380b3d650af31e45640b1c0789e8bbb2e1dc1a2274c972. bash -n passes and no remaining docker-exec|grep -m1 or |head early-close pipelines were found.
Interpretation/next: Retry the same single privileged wrapper invocation. This is a harness-only correction; the Mesa control artifact, Oracle runtime baseline, and experiment hypothesis are unchanged.