Raw-log recovery remains in progress. Correctly named recovery entries now exist, but the legacy file drainer still aborts at the same earlier false-positive safety rejection. A separate new workflow could not use the existing Google identity due to workflow-identity scoping. Recovery must proceed through an already-authorized logging workflow, then the file queue will be hardened.