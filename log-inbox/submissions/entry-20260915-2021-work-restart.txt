Oracle PAYG setup checkpoint: ChatGPT Work consumed its available usage before launching the instance. The premature stop was caused by the assistant-authored instruction to halt on an apparent $4.25/month OCI estimate, even though OCI explicitly states the estimate does not reflect tier unit pricing and the tenancy showed 200 GB of free-tier volume quota available. The Work browser also could not provide the generated private SSH key to the user, so the partially prepared instance cannot be safely launched from that session. Supporting OCI resources remain created and should be reused: VCN al-cloud-vcn, private subnet al-cloud-private-subnet (10.42.1.0/24), NSG al-cloud-instance-nsg, Bastion alCloudBastion. No compute instance, NAT gateway, extra block volume, or load balancer was created. Next: restart instance creation in the user's normal OCI browser, reuse the existing network resources, generate and locally download the SSH key, and launch the intended A1 Flex 4 OCPU / 16 GB Ubuntu 24.04 ARM64 instance with a 100 GB Balanced boot volume and no public IP.