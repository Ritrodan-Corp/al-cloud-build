Validated the observation artifact end-to-end through Remote Desktop Commander: `/usr/local/bin/azl-agent observe` produced `/var/tmp/al-cloud-agent/latest.png`, and the image can be rendered directly through RDC's file reader. The captured Android screen was the launcher/home screen, confirming the narrow interface can hand a delegated worker both compact JSON and the full visual observation without any input action. Next: add a tiny durable probe state and a Work bootstrap instruction that spawns a cheaper subagent and restricts it to this observe+read-file path.