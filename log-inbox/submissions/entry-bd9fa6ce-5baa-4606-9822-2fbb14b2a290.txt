2026-09-17 PDT — Mesa single-thread bottleneck root-cause discriminator identified
Workstream: direct Mesa llvmpipe Oracle performance/menu qualification
Action: Performed read-only host-thread affinity and artifact inspection while the logged-in Mesa session remained live, then checked upstream Mesa llvmpipe thread-control semantics.
Result/evidence: Host game process PID 197756 has 159 threads. Thread-group leader `StarEN.AzurLane` is restricted to CPU 3, but `UnityMain`, `UnityGfxDeviceW`, and all eight sampled `TaskDistributor` threads are allowed on CPUs 0-3. Across all game threads, 91 had affinity 0-3 and 66 had affinity 3. The artifact contains the literal `LP_NUM_THREADS`; the wrapper currently sets `MESA_ANDROID_NO_KMS_SWRAST=1` and `GALLIUM_DRIVER=llvmpipe` but no LP_NUM_THREADS override. The warm CPU sample showed no heavy llvmpipe/raster worker threads, with essentially all rendering CPU in UnityGfxDeviceW. Upstream llvmpipe screen initialization defaults raster worker count from detected CPU count and exposes LP_NUM_THREADS as an integer override; official Mesa documentation states LP_NUM_THREADS controls rendering threads and defaults to CPU count.
Interpretation/next: The graphics thread itself is not affinity-limited, so the current 1-core behavior is likely llvmpipe worker-count/CPU-detection behavior rather than an Android scheduler cap on UnityGfxDeviceW. The next clean performance discriminator is an otherwise identical guarded run with `LP_NUM_THREADS=4`, first checking that raster workers appear and multiple cores are used, then repeating the same home benchmark. Do not change any other renderer variable in that A/B.