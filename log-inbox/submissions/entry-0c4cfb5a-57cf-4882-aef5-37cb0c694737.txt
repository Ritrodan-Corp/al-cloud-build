Browser UI quick fixes deployed live
Deployed two targeted fixes to the running prototype. First, pointer coordinates now map through the actual object-fit: contain video rectangle rather than the full viewport-sized canvas box, so the visible left/right Android edges map correctly to x=0 and x=1279 even with pillarboxing. Pointer-downs in black bars are ignored; active drags clamp safely at the rendered frame edge. Second, the three overlays are no longer linked globally: status/top-left, controls/top-right, and navigation/bottom-center now have independent visibility state. They reveal only from narrow edge-proximity regions, while status itself remains non-interactive and only actual overlay buttons accept pointer events. Backups were saved with stamp 20260915T025751Z. app.js passed node syntax check before deployment.