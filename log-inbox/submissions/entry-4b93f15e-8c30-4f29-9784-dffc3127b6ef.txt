Routine VM STOP no longer carries the 600-second pre-ACPI delay

Workstream: lifecycle control / canon reconciliation

Action:
Reconciled the raw chronology through R000261 with current canonical Project State and Technical Reference, then completed the requested lifecycle change. Workflow `.github/workflows/gcp-vm-chat-control.yml` on `vm-control` was changed at commit `957b5d2f403236fc0d7f5b401991661b976bf470` so routine `stop` is the cleanup-complete path and calls the fixed Compute stop endpoint with `noGracefulShutdown=true`. This bypasses only Compute Engine's configured pre-ACPI application-grace interval; the guest still receives ordinary ACPI soft-off and shuts down through systemd. Added `stop_graceful` as the conservative fallback that retains the configured grace period when cleanup status is unknown. Existing `stop_after_cleanup` remains accepted as a backward-compatible alias to routine `stop`. Routine `stop` succeeds only after STOPPING or TERMINATED is observed. Lifecycle README was reconciled at commit `4b28a24cbb077ae5336383f770af8e78556d1d32`. Canonical Project State and Technical Reference were updated to the same settled contract; the configured 600-second VM setting remains intact only as the fallback policy and is no longer part of routine project closeout.

Live acceptance:
No newer raw entry or logged competing runtime session existed before the test. START commit `3f325201ace310bceb30096810127aa3b98f03ca`, workflow run 34918012833, completed successfully. Guest pre-stop verification found kernel `6.8.12-exp157-39va`; `azl-redroid-exp157.service` active; `azl-control-exp157.service` active; `azl-stream-prototype.service` inactive and disabled; Android `sys.boot_completed=1`; EGL `angle`; Vulkan `pastel`; only loopback listeners on 127.0.0.1:5555 and 127.0.0.1:8765; `/data` mounted from `/var/lib/azl-android/exp157-lowva-data-1`. Routine STOP commit `39e46d55b37dde9a563957f6901a550164660c62`, workflow run 34918092477, then exercised the new default `stop` branch. Compute status was RUNNING at 2026-09-15T01:38:57.327Z, the noGracefulShutdown stop request was accepted at 01:38:57.790Z, and status reached STOPPING at 01:39:03.666Z. This is about 5.9 seconds from observed RUNNING/request time to STOPPING, despite the instance still reporting `gracefulShutdown.maxDuration.seconds=600`. `STOP_READY` was returned only after STOPPING, proving the routine path left the pre-ACPI grace phase without the former ten-minute wait.

Result:
PASS. The requested ten-minute cooldown has been removed from routine project STOP behavior without deleting the VM's conservative graceful-shutdown setting. Normal project closeout is now START -> guest work -> cleanup/baseline verification -> `stop` -> STOPPING/TERMINATED. Use `stop_graceful` only when cleanup status is unknown. `stop_after_cleanup` is a compatibility alias, not the preferred command. No game/client/runtime configuration was altered by this lifecycle change.

Next:
Promote the live acceptance timing into canon, verify stale lifecycle wording is absent, and continue browser-streaming/client validation or agent-control work from the qualified persistent baseline.