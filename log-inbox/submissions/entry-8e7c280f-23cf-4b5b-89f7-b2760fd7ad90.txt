Browser streaming live transport validation passed. With official Azur Lane MainActivity foreground, GET /stream.h264 through the deployed 127.0.0.1:8766 service delivered 1,287,636 bytes during an approximately 8-second client session. Parsed stream contained 239 NALs: SPS=1, PPS=1, IDR=1, non-IDR VCL=236, again matching the expected ~30-coded-picture/sec single-VCL-per-picture pattern. A simultaneous second /stream.h264 request correctly received HTTP 409 `stream already active`, proving single-encoder ownership. After the first client timeout/disconnect, no host adb screenrecord child or guest screenrecord process remained; /api/status returned active=false, bytes_sent=1287636, clients_rejected=1, last_error=null, adb=true. Root GET served the browser UI successfully. This validates live source/HTTP ownership/cleanup but not yet in-browser WebCodecs rendering latency.
